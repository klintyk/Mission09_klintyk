@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model BooksViewModel


<h1> These Books are for Sale </h1>

<!--loop to get all results displayed-->
@*@foreach (Book b in Model.Books)
{
    <h3>@b.Title</h3>
    <ul>
        <li> Author: @b.Author</li>
        <li>Publisher: @b.Publisher</li>
        <li>ISBN: @b.Isbn</li>
        <li> Classification: @b.Classification</li>
        <li> Category: @b.Category</li>
        <li> Page Count: @b.PageCount</li>
        <li>Price: $@b.Price</li>
    </ul>
}*@

<div page-model="@Model.PageInfo" page_action="Index"></div>

@foreach (var b in Model.Books)
{
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @b.Title
            <span class="badge badge-pill badge-primary" style="float:right">
                <small>$@b.Price</small>
            </span>
        </h4>
    </div>
    <div class="card-text p-1"> Author: @b.Author</div>
    <div class="card-text p-1"> Publisher: @b.Publisher</div>
    <div class="card-text p-1">ISBN: @b.Isbn</div>
    <div class="card-text p-1">Classification: @b.Classification</div>
    <div class="card-text p-1">Category: @b.Category</div>
    <div class="card-text p-1"> Number of Pages: @b.PageCount</div>
    <div class="card-text p-1">Price: @b.Price</div>
</div>
}
<div page-model="@Model.PageInfo" page_action="Index" page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" class="btn-group pull-right m-1">
</div>